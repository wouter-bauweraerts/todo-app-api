spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    username: sa
    password:
    url: jdbc:tc:postgresql:latest:///cv
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true