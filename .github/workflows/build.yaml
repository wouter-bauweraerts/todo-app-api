name: build-todo-app-api

on:
  push:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - run: echo $JAVA_HOME
      - run: java -version
      - run: ./mvnw clean package
      # push container to registry with tag Build-${{ github.run_number}}

  deploy:
    runs-on: self-hosted
    needs: build
    steps:
      - uses: actions/checkout@v3

      - run: ls -l

      
      #- working-directory: helm/
      #  run: helm upgrade -i todo-app-api --set todo-app-api.tag=Build-${{ github.run_number}} -n todo-app-api --create-namespace
        
