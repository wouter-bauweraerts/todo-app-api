apiVersion: v1
kind: Service
metadata:
  name: serviceTodoAppApi
  labels:
    app: serviceTodoAppApi
spec:
  type: NodePort
  ports:
  - port: {{ .Values.todoAppApi.port }}
  selector:
    app: "serviceTodoAppApi"
